using UnityEngine;

public class GithubTest : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        // 안녕하세요
        // 지금은 깃허브 연동 테스트 중입니다
        // 연동이 잘 된다면 깃허브 공간에도 해당 내용이 보이겠죠
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
